node_modules/

# Self-signed TLS keypair - regenerated on first run, and key.pem is a private key.
certs/

# Rendered reports on their way to the printer.
tmp/

# Every scanned QR, and under history/ the printed report itself - which means
# every visitor's health results. Never commit.
logs/

# Local render output - preview.pdf holds a real person's measurements.
preview.pdf
preview.png
preview.html
*preview*.pdf
*preview*.png
check-fit*.png
